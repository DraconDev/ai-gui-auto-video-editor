# Project State

## Current Focus
Rename `preset` field to `settings` of type `FolderSettings` in `QueuedFile` struct.

## Completed
- [x] Replace `preset: String,` with `settings: FolderSettings,` in `src/gui.rs`
